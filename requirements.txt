# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Web server ─────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

# ── Computer vision (CV card scanner — Modes 2 & 3) ───────────────────────────
opencv-python>=4.8.0       # card detection, contour analysis, image processing
Pillow>=10.0.0             # image decoding from clipboard paste (Mode 2)
pytesseract>=0.3.10        # OCR wrapper — reads rank text from card corners
mss>=9.0.0                 # OS-level screen capture for live overlay (Mode 3)

# NOTE: pytesseract needs the Tesseract binary (NOT pip):
#   Windows:  choco install tesseract
#             OR https://github.com/UB-Mannheim/tesseract/wiki
#   macOS:    brew install tesseract
#   Linux:    sudo apt install tesseract-ocr
#
# NOTE: mss is only needed for  python main.py overlay  (Mode 3 live overlay).
#       Modes 1 (manual) and 2 (screenshot paste) work without it.
#
# NOTE: tkinter is needed for the live overlay UI. It is part of Python's
#       standard library on Windows and macOS. On Linux run:
#   Ubuntu/Debian: sudo apt install python3-tk
#   Fedora:        sudo dnf install python3-tkinter

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.4.0