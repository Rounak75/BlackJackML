# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Web server ─────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

# ── Computer vision (CV card scanner) ─────────────────────────────────────────
opencv-python>=4.8.0       # card detection, contour analysis, image processing
Pillow>=10.0.0             # image decoding from pasted clipboard screenshots
pytesseract>=0.3.10        # OCR wrapper — reads rank text from card corner images

# NOTE: pytesseract is a Python wrapper around the Tesseract OCR engine.
# You must also install the Tesseract binary separately (it is NOT a pip package):
#
#   Windows:   choco install tesseract
#              OR download installer from https://github.com/UB-Mannheim/tesseract/wiki
#
#   macOS:     brew install tesseract
#
#   Ubuntu/Debian: sudo apt install tesseract-ocr
#
# After installing, verify with:  tesseract --version
# The CV scanner will silently fall back to rank=unknown if Tesseract is missing.

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.4.0